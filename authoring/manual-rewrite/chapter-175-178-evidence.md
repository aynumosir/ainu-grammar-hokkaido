| Review scope | Record |
| --- | --- |
| Chapters and actual TOC slugs | 175 `consolidated-references-bibliography`; 176 `index-of-subjects`; 177 `index-of-grammatical-morphemes`; 178 `index-of-examples-sources-dialects`. |
| Authority and division of work | Parent writes the introductions by hand. The deterministic generator may produce lookup data and validate references; it must preserve authored prose. This file contains audit tables only. No chapter, source, registry, or generator was edited in this audit. |
| Foundation | Preserve the approved person/valency, negation/nominalization/linkage, and TAM/evidentiality matrices. An index must not replace those qualified analyses with a single category or automatic segmentation. |
| Evidence roles | The repository establishes what the site contains and how its links work. Primary linguistic passages establish notation and analysis. KB claims and previous chapter matrices are retrieval pointers, not independent corroboration. Publication citations, original witnesses, and constructed illustrations remain distinguishable. |
| Finding-aid boundary | `AUTHORING.md` §1 explicitly treats the morpheme index as a finding aid, not an internal dictionary. Entries should lead to grammatical treatments. Lexical meanings, etymologies, new paradigms, and inferred homograph analyses are outside this appendix's function. |
| Method and limits | Read the full apparatus generator, four introductions, components and relevant registry fields; sampled the large generated lists. Rechecked the primary passages below. A read-only inventory at one moving-checkout checkpoint found 622 `Ex` blocks, 86 marked constructed, 128 without dialect, 608 without explicit ID, 12 with multiple citations, and 11 constructed examples with citations. These are diagnostic counts, not final publication totals or corpus frequencies. No audio check, external corpus census, or full bibliography re-verification was performed. |

| Source / exact locator | Dialect or scope; evidentiary role | Verified content relevant to the apparatus | Editorial consequence |
| --- | --- | --- | --- |
| Nakagawa 2024 pp. 59–60, table 9; held leaves 57–58 | Saru illustrations; direct explanation of the author's orthographic convention | Table 9 compares `kumokor`, `ku mokor`, `ku-mokor`, `ku=mokor`, and other equivalent notation choices. The text separates his use of equals signs for person marking from the general linguistic clitic-boundary convention. | Do not describe every equals-marked item as a clitic or interpret spaces/hyphens as proof of wordhood. If this point appears in the introduction, cite these pages or refer to the existing notation chapter. Keep the source surface form separate from the site's segmented form. |
| Nakagawa 2024 pp. 165–166, §4.4.2, table 25; held leaves 162–163 | Hokkaido with expressly named regional variants; directly read paradigm and analysis | The author treats person markers as verbal affixes. The table uses `k(u)=` for western Hidaka/Chitose, `ku=` elsewhere, `=as`, `e=`, Ishikari `es=` versus other `eci=`, and `=an`. Third person has no overt form. | Index person-marker spellings and route regional variants to their treatments. Do not fill a missing spelling by analogy, identify every `eci=` with second plural alone, or present zero as a corpus token. |
| Bugaeva 2012 §15.4.1, pp. 472–473; held leaf 8 | Southern Hokkaido; directly read analysis reporting Tamura 1970:305 for diagnostics | Distinguishes word-like `=an`, `=as`, clitic-like `a=`, `eci=`, and other prefixal markers. Ex. 15c has second-plural `eci=`, while ex. 15d has a first-singular-to-second-singular value. | Preserve the disagreement with Nakagawa's classification. Use neutral person-marker terminology in navigation. The index may lead to both uses of `eci=` without inferring the value of every occurrence from its form. |
| Nakagawa 2024 p. 185, §4.5.2; held leaf 182; approved person/valency matrix V14 | Author's nominal-prefix analysis; direct source passage | Lists nominal `e-`, `o-` with arity zero separately from valency-reducing nominal prefixes. The approved matrix distinguishes these from same-shaped applicatives. | An `e-` or `o-` heading needs analysis-specific destinations. A string match does not establish applicative function. Keep `e=` person marking separately searchable. |
| Bugaeva 2015, *An equivalent of the standard of comparison relativization in Ainu*, pp. 48–50 and abbreviation key p. 59; checked for chapters 103–104 | Hokkaido examples with original-source labels; published analysis and reported original examples | The article's examples distinguish AB/Chitose and T1/Saru, among other labels. `tura-no ku=yupo kamuy cotca acapo` is AB 96; `cikir-ihi tanne kikir` is T1 189. | A citation to this article identifies the consulted intermediary, not a new recording by that author. Preserve original locator and dialect in the example's metadata; do not assign one dialect to all its examples. |
| Dal Corso 2020 p. 63 ex. 22, §§5.3.1–5.3.2; checked for chapter 104 | Southern Hokkaido study; original Kayano example reported through the paper | `cisinaot onnay-un sesserke haw-e-as haw-e ene an hi` is credited to Kayano 6-3, 3. The paper proposes an internally headed analysis for this copied-noun construction, with explicit limits. | Source lookup should retain Kayano's witness locator and Dal Corso's intermediary role. Do not infer a more precise dialect from the paper's regional title or count the example as an independently recorded token of Dal Corso. |
| Nikitina & Bugaeva 2021 p. 613 ex. 1b and p. 619 ex. 11a; checked for chapter 105 | Southern Hokkaido reported-speech analysis; published examples with earlier witness locators | Ex. 1b `asinuma arpa-an kusu ne sekor Ø-hawean` reports Tamura 2000:74; ex. 11a `onne-an pe ne kusu a-e-isoytak sekor sino nispa hawean` reports K7908051UP.377. | Keep the original locator and consulted publication distinct. Orthographic/gloss normalization in the grammar does not create a second attestation or establish a competing source transcription. |
| `src/lib/grammar/components/Ex.svelte`, props and citation parsing | Local data/rendering contract; not linguistic evidence | `constructed`, `cite`, `dial`, `place`, `note`, `id`, `m`, `ain`, and `orig` are separate fields. Citation strings may contain several keys and printed-page locators. `id` is optional; the displayed example number is a chapter-local counter. | Reuse those distinctions rather than assigning provenance from the presence of a citation. Preserve source page strings. Individual-example links require a real ID or a documented generated-anchor contract shared with the renderer. |
| `src/lib/grammar/components/Xr.svelte`; `abbreviations.ts` dialect labels | Local navigation and label contracts | `Xr` already supports `s` for a section anchor. `HK` means Hokkaido without further specification; SAR, CHI, ISH, TOK, HOR, SHI, ASA, YAK, SA, KU have registered full labels. | Keep section destinations in the subject data. Use the registered full dialect labels. An absent dialect is not equivalent to HK and must not be silently assigned to Saru. |

| Chapter 175: observed issue | Exact local evidence | Required treatment | Verification |
| --- | --- | --- | --- |
| “Works cited” is broader than the extraction actually establishes | Generator uses every `Object.entries(bibliography)` entry, without scanning all prose citations; `/grammar/references` also renders the registered bibliography. | Either describe the scope as the registered bibliography or derive the cited set from every supported citation component plus expressly retained original witnesses. Scanning `Ex` alone would omit prose-only references. | Compare displayed keys against the declared inclusion rule, including a prose-only source and an original cited through an intermediary. |
| Two renderings can disagree | Chapter 175 generator duplicates formatting and omits `url`, `note`, and per-key anchors; the running references route has those fields and anchors. | Share bibliography data and preferably one rendering/formatting implementation. Keep the chapter's place in the 178-chapter sequence while ensuring citation links resolve to one authoritative entry. | Compare representative book, article, edited-volume chapter, non-English title, reported original, and archive entries between views. |
| “Reported” is a claim about access, not a permanent property of a work | Intro says reported entries were not directly consulted; the registry separates default evidence role and local holdings. Different chapters can use a publication differently. | Keep intermediary attribution at the particular claim/example. Reconcile global badges with actual direct checks; local possession alone does not mean a work was read or supplies direct evidence for every cited claim. | Spot-check a directly read work and a genuinely unconsulted original. Do not automatically clear every reported flag based on a file existing. |
| Lookup labels can collapse separate works | Source index labels use only `citeAuthor` and `year`; keys such as `sato2009` / `sato2009yukar` and `nakagawa2022` / `nakagawa2022pos` are distinct. | Disambiguate with the short title, a consistently assigned year suffix, or equivalent visible identifying detail, and link the exact bibliography key. | Two works by the same author in the same year must remain distinguishable in the list and linked destination. |
| Metadata corrections already found must survive regeneration | Chapter 103 matrix distinguished two Bugaeva 2015 works. Current `bugaeva2015` entry now correctly identifies *An equivalent of the standard of comparison relativization in Ainu*. Chapters 157–162 matrix records additional verified bibliographic corrections. | Preserve the corrected identity. Do not reintroduce the different *Relative Clauses and Noun Complements in Ainu* title under this key or carry that publication's pp. 79–100 into citations to the pp. 43–61 article. Apply other corrections from their actual audit rows before final generation. | Verify title, exact key, locators, KB source identity and canonical link together. This audit found the 2015 title corrected in the live bibliography. |
| Holdings metadata remains stale in concrete entries | `citation-registry.ts` still marks `simeon1968` and `hattori1964` not held; both full PDFs were directly examined in chapter 157–162 audit. Tamura 1996 dictionary text is available in the sibling dictionaries repository. | Update verified holdings with correct relative assets. For the dictionary, describe the actual digital text rather than claiming a separately checked full printed edition. Keep internal paths out of public bibliography notes. | Resolve each corrected path; compare with the source inventory in the earlier audit. No blanket holdings rewrite. |
| Minimal Simeon holdings repair | Existing `simeon1968` record has the correct work key and `prior-analysis` role but `heldLocally: false`. Full PDF exists at `articles/1968_Simeon_The-Phonemics-and-Morphology-of-Hokkaido-Ainu.pdf` relative to `SOURCE_REPO`. | Set `heldLocally: true` and add that exact relative `path`; retain `dbSlug` and `sourceRole`. The earlier audit directly read the title and pp. 13–17, including the p. 16 image. | Path existence rechecked for this appendix audit. Do not infer whole-work reading from the checked passages or change unrelated publication metadata automatically. |
| Minimal Hattori holdings repair | Existing `hattori1964` record has `primary-data` role but `heldLocally: false`. Full PDF exists at `books/1964_Hattori_HogenJiten/服部四郎編『アイヌ語方言辞典』.pdf` relative to `SOURCE_REPO`; accompanying OCR has the same stem plus `.gpt-5.4.txt`. | Set `heldLocally: true` and add the exact PDF `path`; retain `sourceRole` and existing citation identity. The earlier audit directly checked the p. 37 image and relevant pp. 37–38 text. | Both files' existence rechecked. Link the actual edition asset, not a guessed `books/1964_Hattori` directory, and do not describe OCR as independent confirmation. |
| Reader-visible workflow notes | `bibliography.ts` entries `endo2022` and `okuda2022` contain “Read in full (epub→pandoc …)”; `/grammar/references` prints `note`. `shiraishitangiku2022` also has a reading-status note. | Remove reading status and conversion-tool details from displayed notes. Retain useful publication facts or section-based citation guidance where needed. Reading records belong in internal evidence files. | Inspect rendered bibliography notes and search the public data for editing/conversion status. |

| Chapter 176: observed issue | Local evidence | Practical lookup requirement | Check |
| --- | --- | --- | --- |
| Heading dump is a weak subject index | Generator takes `<S t=…>` verbatim, producing sentence-like entries and general headings rather than stable grammatical terms. | Use manually chosen canonical subject terms and aliases with destinations; chapter titles and section headings can supply additional navigation. New index terms must name treatments already present, not introduce new analyses. | A reader can find person indexing, possession, incorporation, nominalization, evidentiality, negation, and reported speech under those terms. |
| Known section destinations are discarded | Scanner records `id`, but aggregation keeps only `(term, slug)` and emits chapter-only `Xr`. | Preserve `(term, slug, sectionId)` where the target section exists; link with `Xr s`. Dedupe identical destinations, not every same-word heading in a chapter. | Every retained section ID resolves to that chapter. A repeated term can point to two different sections without one disappearing. |
| Chapter-title coverage is promised only in a code comment | Generator header says “section headings + chapter titles”; scan never adds the chapter title. | Include chapter titles explicitly if that remains the intended fallback. Keep the introduction consistent with the implemented coverage. | Compare TOC chapter coverage against index destinations after the final rewrite. |
| Alternative analytical terms need searchable aliases | Approved foundation matrices preserve disagreements about marker status, fourth-person functions, voice, nominalization, and TAM. | Link aliases to the same explanatory treatment without equating the analyses. Examples: fourth/indefinite/logophoric person; affiliative/possessive form; nominalizer/formal noun; quasi-incorporation. | Aliases lead to qualified chapter discussions; they do not silently replace disputed labels with a single definition. |
| Quasi-incorporation must retain the word/phrase distinction | User steering and approved person/valency treatment; source specifics in chapter 88–90 and 91–96 evidence matrices | Provide a separate destination for quasi-incorporation where the authored chapter distinguishes it. Do not merge every multiword sequence into noun incorporation because both contain the same nominal form. | Inspect the canonical term and its destination after that chapter is finalized. No new quasi-incorporation analysis is supplied by the index. |

| Suggested canonical subject group | Verified existing chapter destinations | Alias / boundary to preserve |
| --- | --- | --- |
| Word classes; property predicates | `word-class-inventory-and-diagnostics`; `no-adjective-class-property-verbs`; `property-concept-predication` | Adjective terminology points to the classification discussion. |
| Possession; affiliative forms | `alienable-inalienable-split`; `concept-form-affiliative-form`; `affiliative-suffix-morphophonology`; `adnominal-possession-double-marking` | Separate form, construction, and semantic possession. |
| Person indexing; alignment | `person-marking-architecture`; `personal-affix-template-and-ordering`; `alignment-split-intransitive`; `alignment-hierarchical-and-the-debate` | Agreement/indexing aliases do not settle the affix/clitic/word debate. |
| Fourth person; clusivity; portmanteau | `indefinite-fourth-person-paradigm`; `first-person-plural-and-clusivity`; `eci-portmanteau-and-second-plural`; `fourth-person-honorific` | Distinguish morphological paradigm from contextual reference. |
| Valency; causatives; applicatives | `valency-voice-overview`; `causative-morphological-re-e-te`; `applicative-system-overview`; `causee-marking-ditransitive-double-causative` | Argument count is not the number of overt person markers. |
| Incorporation; quasi-incorporation | `noun-incorporation-object`; `noun-incorporation-subject-possessor-stranding`; `noun-incorporation-oblique-polysynthesis-debate`; `adverbial-incorporation` | Keep source-specific wordhood diagnostics visible at destinations. |
| Nominalization; relative clauses; complements | `nominalization-p-pe`; `lexical-head-formal-noun-nominalization`; `prenominal-gap-relative-clauses`; `headless-internally-headed-noun-modifying-clauses`; `nominalized-complements-control-raising` | Gapless complementation and internally headed relative analyses are distinct. |
| Time reference; aspect | `tenselessness-and-time-reference`; `existential-aspect-an-oka`; `perfective-anterior-a`; `phasal-iterative-habitual-aspect` | Perfect/perfective/anterior labels point to attributed treatments. |
| Evidentiality; mirativity | `evidential-schema-overview`; `ruwe-ne-inferential`; `siri-ne-situational`; `hawe-ne-reportative`; `humi-ne-sensory`; `mirativity-scope-grammaticalization` | Do not label the four construction names as four universally agreed meanings. |
| Negation; prohibition | `negation-somo-standard-clausal`; `negation-existential-possessive-isam`; `negation-polarity-items-scope`; `negation-prohibitive-iteki` | Separate negative constructions and polarity items. |
| Clause linkage; reference tracking | `clause-linkage-overview`; `sequential-wa-and-clause-chaining`; `hine-akusu-switch-reference`; `conditional-temporal-concessive-clauses`; `reference-tracking` | A legacy slug containing “switch-reference” does not authorize a categorical analysis in the visible index label. |
| Reported speech; oral genres; register | `quotative-complementation-sekor`; `logophoric-narration-and-reported-discourse`; `oral-literature-genre-taxonomy`; `sakehe-refrain-and-sung-verse-structure`; `honorific-ritual-and-taboo-registers` | Link language structure and genre treatments separately. |

| Chapter 177: observed issue | Exact evidence | Required treatment | Check |
| --- | --- | --- | --- |
| Whole words are labeled morphemes | Generator retains the entire whitespace token if it contains `=` or is a short hyphenated string. Current list includes `a=aca`, `a=e-hotke`, `a=mi-p`, and many other complete words. | Use a curated inventory of grammatical forms with main discussion destinations. If occurrence links are retained, extract only explicitly segmented, reviewed grammatical components; distinguish them from whole-word surface-form lookup. | `a=e-hotke` can lead to entries for `a=` and applicative `e-` only when the reviewed segmentation licenses them. It must not become a morpheme entry itself. |
| Length and punctuation are treated as linguistic criteria | `t.includes('-') && t.length <= 8`; every equals-marked token passes. | Remove the arbitrary length gate and punctuation-only classification. A validated allowlist is adequate for this finding aid; automatic inference of roots, categories, or allomorphy is unnecessary. | A long grammatical form remains findable; a short lexical compound does not become a grammatical morpheme solely because it has a hyphen. |
| Essential forms are missed by the seed list | At the checked checkpoint, standalone `a=`, `e=`, `e-`, `-ihi`, `aan`, `iteki`, and `rusuy` had no entries; `=an` appeared only where separately tokenized. | Include the grammar's main taught forms through curated entry data and chapter destinations, independently of whether the present `Ex` sample happens to contain a convenient token. | Check the representative forms listed here after generation; inventory completeness is defined by the approved lookup list, not a claim to enumerate all Ainu morphology. |
| “Person clitics” contradicts the acknowledged notation and disagreement | Chapter introduction; Nakagawa pp. 59–60; Bugaeva pp. 472–473; `Ex` comment explicitly calls `=` an orthographic convention. | Use person-marker terminology and a concise pointer to the notation/status discussion. Do not turn the index's punctuation legend into a linguistic diagnosis. | Introduction and legends agree with approved foundation T1–T2. |
| “Attested usage” includes constructed examples | Morpheme extraction ignores `constructed`. | Describe examples as examples, or visibly separate attested/published evidence from constructed illustrations. A citation attached to a construction does not make it a recorded utterance. | A constructed-only form cannot acquire an attestation label through index generation. |
| Homographs and analyses are collapsed | All normalized identical strings share one `Set<chapter>`. Person `e=`, nominal `e-`, applicative `e-`; existential `an` and person `=an`; auxiliary `a` and person `a=` need distinct destinations. | Preserve boundary notation; provide analysis-specific subentries or qualified destination labels where the same spelling has distinct treatments. Do not infer historical identity from spelling or unite a phrase with a word. | Test `e= / e-`, `an / =an`, `a / a=`, and copular/anaphoric `ne`. |
| Lowercasing and accent-stripping can hide distinctions | Sorting strips combining accents and lowercases; entry keys are lowercased. | Normalization may assist ordering/search, while display forms and analysis-specific identities remain intact. An accented form is not evidence for a new morpheme; removing accent is not proof of identity. | Preserve original display spelling; establish an explicit stable tie-break for equal normalized keys. |
| Index risks becoming a lexical glossary | Existing introduction says lexical roots are omitted, but retained tokens contain roots; AUTHORING §1 prohibits internal dictionary work. | Prefer form, short grammatical navigation label if needed, and links. Keep dictionary meanings and lexical entries in the external dictionary. | No unsourced sense lists, productive paradigms, or etymologies added during generation. |

| Minimum useful grammatical-form lookup family | Destinations already present | Handling constraint |
| --- | --- | --- |
| `ku=`, `k=`, `e=`, `en=`, `ci=`, `un=`, `eci=`, `a=`, `i=`, `=as`, `=an` | Chapters 62–70, particularly `first-second-person-singular-affixes`, `eci-portmanteau-and-second-plural`, `indefinite-fourth-person-paradigm` | These are lookup spellings, not a newly asserted pan-Hokkaido paradigm. Keep dialect variants and portmanteau uses at their source-backed destinations. |
| Affiliative endings, including `-ihi` and the forms actually taught in chapter 45 | `concept-form-affiliative-form`; `affiliative-suffix-morphophonology` | Curate from the finalized chapter; do not manufacture a complete allomorph inventory by splitting every hyphenated token. |
| `-re`, `-e`, `-te`, `-ka`, `yar`; `e-`, `ko-`, `o-`; `i-`, `yay-`, `si-`, `u-`, `ci-` | Chapters 74–87 and the relevant derivation/nominal-prefix treatment | Separate identical strings with different analyses. Do not label every `-e` causative or every `ci-` person indexing. |
| `p/pe`, `hi/i`, `kur`, `usi`; `no` | `nominalization-p-pe`; `lexical-head-formal-noun-nominalization`; `adverbializer-no` | Display the variants and word/affix analyses used by the cited treatment. A slash is a finding-aid grouping, not an automatic distribution rule. |
| `an/oka`, `a/rok`, `aan`, `rusuy`; `ruwe ne`, `siri ne`, `hawe ne`, `humi ne` | Chapters 97, 110–125 | Construction phrases can be indexed as constructions with separate labels. Do not call every listed expression one morpheme. |
| `somo`, `isam`, `iteki`; `wa`, `hine`, `akusu`, `kor`, `yak`, `yakka`, `kusu`, `kuni`, `sekor` | Chapters 105–107 and 126–137 | Link grammatical functions without automatically equating same-shaped lexical verbs, particles, and constructions. |
| `anak/anakne`, question/final particles actually taught in chapters 140 and 143–144 | `topic-marking-anakne`; `sentence-final-particles-illocutionary-force`; `interrogative-strategies-question-particles` | Retain the final chapter's dialect and distribution qualifications; the index supplies destinations, not a universal sentence-final paradigm. |

| Chapter 178: observed issue | Exact local/source evidence | Required treatment | Check |
| --- | --- | --- | --- |
| Citations are called sources from which examples were drawn | Generator counts every `cite` key and explicitly attributes constructed examples to a source whose analysis they illustrate. Eleven constructed examples had citations at the inventory checkpoint. | Label the group by cited publication unless the metadata establishes the original witness. Keep `constructed` visible; distinguish a source author's published constructed diagnostic from a new editorial illustration when the example note supplies that fact. | A constructed example with a citation stays constructed. A source analysis citation is not relabeled as an original utterance witness. |
| Original and intermediary sources are flattened | Chapters 103–105 examples above retain original Bugaeva 2004, Tamura, Kayano or archive locators in notes/place while `cite` names the directly consulted study. | Preserve the existing provenance chain. A compact index may link the example where that chain is explained; it need not invent new source records from free text. | Check one original-publication citation, one cited-through example, and one archive excerpt. |
| Chapter counts do not locate individual examples | Current source rows contain counts and chapter-only links; most `Ex` blocks lack explicit IDs. | At minimum retain a clear source-by-chapter locator. For a genuine individual-example index, use stable example anchors shared with `Ex`, keep the current displayed chapter-local number as a label, and carry source pages/dialect/construction status. | Link to the correct example after inserting an earlier example in the same chapter. If that capability is deferred, do not claim the table links individual examples. |
| Dialect rows are unexplained codes without destinations | Generator emits code and count only. Missing dialects are omitted, while `HK` has its own defined meaning. | Use registered full names with codes and example/chapter destinations. Keep an explicit unspecified bucket if displaying a total distribution; do not fill missing tags from a source title, author, or geographical bibliography group. | HK and absent labels stay separate. SA/KU comparisons remain identifiable and are not silently counted as Hokkaido examples. |
| Counts are overlapping, not an exhaustive corpus census | One example with two citations contributes to two source totals; examples may lack dialect metadata; the same historical utterance may be reused across chapters. | Define counts as local rendered example occurrences and state overlaps where totals are displayed. They measure the grammar's illustration set, not Ainu frequency, speaker distribution, or independent attestations. | Sum unique example records once; derive grouped counts from those records. Multiple-source totals need not equal the unique-example count. |
| Source locators are thrown away during aggregation | Generator strips `:pages` to obtain the bibliography key and does not retain the locator. | Keep the full locator in example data even when grouping by key. Chapter/section locators for reflowable Handbook sources remain valid; do not replace them with invented print pages. | A multi-source example retains both independent locator strings; same-key repeated references dedupe within that example only when appropriate. |
| Publication sample may be mistaken for a new corpus check | Existing wording calls the extracted set an example corpus and the morpheme index calls it attested usage. | Keep the distinction between examples printed in this grammar and the external corpora underlying some sources. The index does not certify audio, authenticity, or independent token uniqueness. | No newly generated “corpus-confirmed,” frequency-rank, or universal attestation claims. |

| Generator boundary / acceptance check | Concrete implementation requirement | Expected evidence at completion |
| --- | --- | --- |
| Authored prose is outside generated output | Store generated lookup arrays in dedicated data modules, or constrain replacement to unambiguous marked data blocks. Do not reconstruct the four complete chapter files from generator-owned prose templates. | Running the generator leaves manually written introduction text byte-for-byte unchanged. No external model call is involved. |
| One input model, explicit types | Represent subject targets, grammatical-form targets and example records explicitly. Keep a small curated grammatical lookup inventory separate from an optional occurrence index. | Invalid chapter IDs, missing sections, unknown bibliography keys, unknown dialect codes, and duplicate IDs fail validation with actionable errors. |
| Parse the supported Svelte syntax correctly | Use the installed Svelte parser or enforce and validate a deliberately limited static-attribute contract. Existing regex matching can silently miss non-double-quoted/expression attributes and mishandle special text. | Fixtures cover multiline tags, entities, multiple citations, `constructed`, explicit true/false, and omitted optional fields. Unsupported dynamic metadata fails clearly instead of generating plausible incomplete counts. |
| Do not scrape analysis from prose | Generator may copy validated fields, sort, group, count, and link. It must not infer dialect, acceptability, glosses, morpheme function, wordhood, original witness, or evidentiary status from arbitrary strings. | The reviewed curated data supplies those distinctions where the lookup needs them. Unspecified metadata remains unspecified. |
| Meaningful morpheme regression cases | Use reviewed examples containing `a=e-hotke`, `cikir-ihi`, person `e=`, nominal/applicative `e-`, and the independent existential `an` versus person `=an`. | Correct finding-aid entries and destinations; complete words are not returned as grammatical morphemes, and unsegmented lexical words are not automatically decomposed. |
| Meaningful provenance/count regression cases | Include a cited constructed diagnostic, an uncited editorial illustration, one example with two citations, one missing dialect, and a cited-through example. | Status and locators survive extraction; the unique count is stable; source groups may overlap; missing dialect remains visible or explicitly outside a labeled subtotal. |
| Determinism and freshness | Generate after final chapter rewrites and metadata corrections. Stable sort includes a deterministic tie-break. A check mode may compare expected output with checked-in data without mutation. | A second run produces no diff; changes are limited to intended lookup data. Final totals derive from the finalized files, not this audit's temporary counts. |
| Navigation and rendering | Validate every subject/morpheme destination and bibliography anchor; inspect one small viewport for long source labels and index rows. Use existing project checks/build. | No broken chapter/section/example links, malformed entities, duplicate anchor IDs, or clipped lookup labels. No deployment is required for review. |
| Final source-status limitation | This audit verifies generator semantics, sampled metadata, and the cited primary passages. Other matrices contain publication-specific corrections; hundreds of bibliography entries were not individually rechecked here. | Report the apparatus as mechanically checked against the declared input contracts. Do not report a newly completed full-bibliography or corpus audit. |
